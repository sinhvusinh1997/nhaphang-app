import {StyleSheet, Text, View} from 'react-native';

export const Home = () => {
  console.log('in home');

  return (
    <View>
      <Text>Home page</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
